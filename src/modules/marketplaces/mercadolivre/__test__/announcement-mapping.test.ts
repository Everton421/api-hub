import test from "node:test";
import { MlAnnouncementMapping } from "../announcement/mapping/ml-announcement-mapping.ts";

test("TESTE", async (t)=>{
    await t.test(" MAPPING ANNOUNCEMENT ", async ()=>{
        const mapping = new MlAnnouncementMapping();
    })
})