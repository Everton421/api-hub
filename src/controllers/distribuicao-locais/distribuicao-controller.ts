import { Request, Response } from "express";

export class DistribuicaoController{


    async update(req:Request, res:Response ){

            /// ****
            // recceber os dados, buscar o produto + local + setor, se encontrar comparar a data de recadastro
            // atualizar caso necessario 
            // inserir caso nao existir  
    }

}